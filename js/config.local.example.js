/**
 * Local config template — copy to js/config.local.js and fill in your token.
 *
 *   cp js/config.local.example.js js/config.local.js
 *
 * config.local.js is gitignored, so your token never gets committed.
 * Get a free WAQI token at https://aqicn.org/data-platform/token/.
 *
 * window.WAQI_TOKEN takes precedence over localStorage.waqi_token.
 */
window.WAQI_TOKEN = 'YOUR_WAQI_TOKEN_HERE';
